{
  "name": "observability-foundations-api",
  "version": "1.0.0",
  "private": true,
  "description": "Instrumented Node.js API for logs, health checks and Prometheus metrics",
  "main": "src/server.js",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "node src/server.js",
    "lint": "node --check src/server.js && node --check test/server.test.js",
    "test": "node --test",
    "ci:local": "npm run lint && npm test"
  }
}